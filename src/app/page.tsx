import { LandingPage } from '@/components/landing/landing-page';

export const metadata = {
  title: 'Buzzbox — Local-first marketing operations command center',
  description:
    'CRM, outreach, content, agents, and analytics in one self-hosted cockpit. Agents do the busywork; humans keep the pen.',
};

export default function BuzzboxLanding() {
  return <LandingPage />;
}
